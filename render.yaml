services:
  - type: web
    name: denis-3d-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    pullRequestPreviews:
      autoDeploy: false
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: OWNER_ID
        sync: false
      - key: WEBAPP_URL
        sync: false
      - key: RENDER_URL
        sync: false
      - key: PORT
        value: 8443
      - key: DB_BACKEND
        value: sqlite
      - key: DB_FILE
        value: /var/data/users.db
      - key: DATABASE_URL
        sync: false