# 📊 Адмін панель для управління товарами

## Огляд
Сучасна адмін панель для управління товарами магазину 3D друків прямо в Telegram через `/admin` команду. Панель дозволяє легко додавати, редагувати та видаляти товари, завантажувати фото на Cloudinary, налаштовувати акції та персоналізовані товари.

---

## 🔧 Налаштування

### 1. Встановлення залежностей

```bash
pip install -r requirements.txt
```

**Потрібні пакети:**
- `cloudinary` — зберігання фото
- `Pillow` — обробка зображень
- `python-telegram-bot` — Telegram бот
- `aiohttp` — HTTP сервер

### 2. Налаштування WebApp для Telegram

**Крок 1: Налаштувати .env файл**

1. Скопіюйте `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Додайте `ADMIN_WEBAPP_URL` залежно від середовища:

**Локально (розробка):**
```env
ADMIN_WEBAPP_URL=http://localhost:8080/admin/panel
```

**На Render/публічному сервері:**
```env
ADMIN_WEBAPP_URL=https://your-app-name.onrender.com/admin/panel
```

> ⚠️ **Важливо:** Це повинна бути публічна HTTP (S) адреса, яка доступна з інтернету!

### 3. Налаштування Cloudinary

**Крок 1: Зареєструватися на Cloudinary**
1. Перейти на https://cloudinary.com/users/register/free
2. Заповнити форму реєстрації (email, пароль)
3. Підтвердити email

**Крок 2: Отримати API ключі**
1. Перейти на https://cloudinary.com/console
2. На сторінці Dashboard скопіювати:
   - **Cloud Name** (формат: `xxxxx`)
   - **API Key** (цифри, наприклад: `452626753771953`)
   - **API Secret** (довгий ключ)

**Крок 3: Додати в .env**
```env
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
```

> ⚠️ **Важливо:** Ніколи не комітьте `.env` файл з реальними ключами в git!

### 4. Запуск бота

```bash
python bot.py
```

**Розташування логів:**
- Бот запуститься на порту `8080`
- HTTP сервер буде слухати на адресі з `ADMIN_WEBAPP_URL`
- Логи виводитимуться в консоль

---

## ✅ Етап 3: Telegram WebApp — ЗАВЕРШЕНО

### Тестування в Telegram

1. **Запустіть бот:**
   ```bash
   python bot.py
   ```

2. **Надішліть команду боту в Telegram:**
   ```
   /admin
   ```

3. **Натисніть кнопку "📊 Адмін панель"**
   - Панель відкриється в WebApp прямо в Telegram
   - На мобільному: буде в повноекранному режимі
   - На десктопі: буде в модальному вікні

### Налаштування для Render / публічного сервера

Якщо ви розміщуєте бот на Render чи іншому серверу:

1. **Отримайте публічний URL** (наприклад: `https://my-app.onrender.com`)

2. **Додайте в Render環境變數:**
   ```
   ADMIN_WEBAPP_URL=https://my-app.onrender.com/admin/panel
   ```

3. **Перезавантажте бот** — WebApp буде доступна з Telegram

---

## ✅ Етап 4: Інтеграція Cloudinary — ЗАВЕРШЕНО

### Налаштування (вже готово)

Cloudinary налаштована з наступними параметрами:
```env
CLOUDINARY_CLOUD_NAME=df5stvc1c
CLOUDINARY_API_KEY=452626753771953
CLOUDINARY_API_SECRET=Tyfsv4pkkdu3bxQyuEeKMVd_dJE
```

### Можливості завантаження фото

✅ **Автоматична оптимізація:**
- Масштабування до 1000px ширини
- Якість 85% (баланс розміру і якості)
- Автоматичний формат (WebP для сучасних браузерів)

✅ **Обробка помилок:**
- Перевірка типу файлу (тільки зображення)
- Обмеження розміру (максимум 25 MB)
- Детальні повідомлення про помилки

✅ **API відповідь включає:**
```json
{
  "ok": true,
  "url": "https://res.cloudinary.com/df5stvc1c/image/upload/...",
  "width": 1000,
  "height": 667,
  "size": 245000
}
```

### Тестування завантаження

1. Відкрийте адмін панель через `/admin` в Telegram
2. Натисніть "➕ Додати товар"
3. Перетягніть фото в область **"📸 Перетягни фото сюди"**
4. Фото автоматично завантажиться на Cloudinary
5. Ви побачите превью фото перед збереженням товару

### Папка в Cloudinary

Всі фото зберігаються в папці: **`poselyanov3dprint/`**

Формат публічного ID: `product_photo_1716103800` (filename + timestamp)

---

1. **В Telegram:**
   - Надішліть команду `/admin` боту
   - Натисніть кнопку "📊 Адмін панель"
   - Панель відкриється в WebApp прямо в Telegram

2. **Локально (для розробки):**
   - Перейти на `http://localhost:8080/admin/panel`
   - Панель відкриється в браузері

### Основні функції

#### ➕ Додавання товару

1. Натисніть кнопку "➕ Додати товар"
2. Заповніть форму:
   - **Назва товару** — назва для каталогу
   - **Категорія** — вибір типу товару:
     - 🧸 Іграшка (toy)
     - 🔑 Брелок (key)
     - ✋ Антистрес (anti)
     - 🎨 Персоналізований (custom)
   - **Emoji** — один смайл для товару (максимум 2 символи)
   - **Ціна** — ціна в гривнях
   - **Стара ціна** — для розраховування знижки на акціях
   - **Матеріал** — опис матеріалу (наприклад: "PLA · один колір")

3. **Опції товару** (для не персоналізованих):
   - 🔥 **Гарячий товар** — показуватиме як популярний
   - 🎁 **Може бути подарунком** — для подарункової опції (тільки для дешевих, ≤100₴)
   - 🎨 **Вибір кольору** — дозволяє вибір філаменту при замовленні

4. **Для персоналізованих товарів:**
   - **Поле для вводу** — описує, що вводить користувач при замовленні
   - Приклади: "Номер автомобіля", "Ім'я", "Ініціали", тощо

5. **Завантаження фото:**
   - Перетягніть фото в область drag & drop, або натисніть для вибору
   - Фото автоматично завантажиться на Cloudinary
   - Можна додавати кілька фото до одного товару
   - Натисніть ✕ щоб видалити фото перед збереженням

6. Натисніть "Зберегти товар"
7. Товар з'явиться в каталозі та панелі

#### ✏️ Редагування товару

1. Натисніть кнопку "✏️ Редагувати" на товарі
2. Змініть потрібні поля
3. Можна додавати нові фото або видаляти старі
4. Натисніть "Зберегти товар"

#### 🗑️ Видалення товару

1. Натисніть кнопку "🗑️ Видалити" на товарі
2. Підтвердіть видалення в діалозі
3. Товар видалиться з каталогу

---

## 📂 Структура даних

### Товари звичайні (`products.json`)

```json
{
  "id": 1,
  "cat": "toy",
  "emoji": "🐉",
  "photos": [
    "https://res.cloudinary.com/...",
    "https://res.cloudinary.com/..."
  ],
  "name": "Fidget-спіраль",
  "mat": "PLA · один або два кольори",
  "price": 300,
  "oldPrice": 150,
  "hot": true,
  "gift": false,
  "filamentChoice": true
}
```

**Поля:**
- `id` — унікальний ідентифікатор (авто-генерується)
- `cat` — категорія (toy, key, anti, custom)
- `emoji` — смайл для товару
- `photos` — масив посилань на фото в Cloudinary
- `name` — назва товару
- `mat` — матеріал / опис
- `price` — поточна ціна
- `oldPrice` — (опціонально) стара ціна для акцій
- `hot` — позначка популярного товару
- `gift` — може бути подарунком
- `filamentChoice` — можна вибрати колір філаменту

### Персоналізовані товари (`custom_products.json`)

```json
{
  "id": 101,
  "cat": "custom",
  "emoji": "🔑",
  "photos": [],
  "name": "Брелок з номером авто",
  "mat": "PLA · персоналізований",
  "price": 70,
  "oldPrice": 90,
  "hot": false,
  "custom_fields": "Номер автомобіля"
}
```

**Відмінності:**
- `cat` завжди `"custom"`
- `custom_fields` — опис поля для вводу користувачем
- `gift` не використовується (персоналізовані не можуть бути подарунками)

### Філаменти (`filaments.json`)

```json
{
  "id": "white",
  "name": "Білий",
  "hex": "#f0f0f0",
  "available": true
}
```

---

## 🔐 Безпека

### Аутентифікація

Адмін панель доступна **тільки** адміну бота (ID визначається в `OWNER_ID` в `.env`):

```env
OWNER_ID=718746623  # Ваш Telegram ID
```

**Як отримати свій ID:**
- Надішліть команду `/myid` боту
- Бот поверне вашу ID

### Перевірка доступу

Кожен API запит перевіряє:
1. Чи користувач є адміном
2. Чи запит поточує від дозволеного джерела (CORS)
3. Чи дані коректні (валідація)

---

## 🌐 API Endpoints

### GET `/admin/panel`
Отримати HTML адмін панелі з рішення в браузері.

**Відповідь:** HTML сторінка з адмін панеллю

### GET `/api/products`
Отримати список всіх товарів (звичайні + персоналізовані).

**Відповідь:**
```json
[
  {
    "id": 1,
    "name": "Fidget-спіраль",
    "price": 300,
    ...
  }
]
```

### GET `/api/products/{id}`
Отримати один товар за ID.

**Параметри:**
- `id` — ID товару

**Відповідь:**
```json
{
  "id": 1,
  "name": "Fidget-спіраль",
  ...
}
```

### POST `/api/products`
Створити новий товар.

**Тіло запиту:**
```json
{
  "name": "Новий товар",
  "cat": "toy",
  "emoji": "🎮",
  "price": 150,
  "mat": "PLA",
  "photos": ["https://..."],
  "hot": false,
  "gift": false,
  "filamentChoice": true
}
```

**Відповідь:**
```json
{
  "ok": true,
  "id": 5
}
```

### PUT `/api/products/{id}`
Редагувати існуючий товар.

**Параметри:**
- `id` — ID товару

**Тіло запиту:** (як при створенні, але тільки змінені поля)

**Відповідь:**
```json
{
  "ok": true
}
```

### DELETE `/api/products/{id}`
Видалити товар.

**Параметри:**
- `id` — ID товару

**Відповідь:**
```json
{
  "ok": true
}
```

### POST `/api/upload-photo`
Завантажити фото на Cloudinary з автоматичною оптимізацією.

**Тіло запиту:** (multipart/form-data)
- `file` — файл фото (максимум 25 MB, тільки зображення)

**Успішна відповідь (200):**
```json
{
  "ok": true,
  "url": "https://res.cloudinary.com/df5stvc1c/image/upload/...",
  "width": 1000,
  "height": 667,
  "size": 245000
}
```

**Помилка (400):**
```json
{
  "ok": false,
  "error": "❌ Файл занадто великий (50.5MB). Максимум 25MB"
}
```

---

## 🎨 Особливості інтерфейсу

### Дизайн
- **Сучасний градієнт** з фіолетовою палітрою
- **Темна тема** для зручності при роботі
- **Адаптивний дизайн** — працює на мобільних та десктопі
- **Плавні анімації** для кращого користувацького досвіду

### Компоненти

#### Карточка товару
```
┌─────────────────────────┐
│ 🐉 Fidget-спіраль      │
│    toy                  │
│                         │
│ 300 ₴                   │
│                         │
│ [🖼️] [🖼️] [🖼️]         │
│                         │
│ [✏️ Редагувати] [🗑️ Видалити] │
└─────────────────────────┘
```

#### Форма додавання
- Текстові поля для введення
- Dropdown для вибору категорії
- Drag & drop зона для фото
- Превью завантажених фото
- Checkbox'и для опцій

#### Toast повідомлення
- ✅ Зелене повідомлення про успіх
- ❌ Червоне повідомлення про помилку
- Автоматично зникають через 3 секунди

---

## 🚀 Про що готуватися

### Etap 1: Бекенд ✅ ЗАВЕРШЕНО
- Функції для роботи з товарами
- HTTP API handlers
- Cloudinary інтеграція
- Команда `/admin`

### Etap 2: Фронтенд ✅ ЗАВЕРШЕНО
- HTML адмін панель (вже включена в handler'і)
- JavaScript для CRUD операцій
- Drag & drop для фото
- Валідація форм
- Toast повідомлення

### Etap 3: Telegram команда ✅ ЗАВЕРШЕНО
- Додавання команди в меню ✅
- WebApp інтеграція ✅ (адаптивний URL через .env)
- Тестування в Telegram ✅

### Etap 4: Інтеграція Cloudinary ✅ ЗАВЕРШЕНО
- ✅ Налаштування API ключів (конфіг готовий)
- ✅ Завантаження фото (реалізовано з обробкою помилок)
- ✅ Оптимізація розміру (масштабування до 1000px, якість 85%, автоформат WebP)

---

## 📝 Приклади використання

### Приклад 1: Додавання простого товару

```
1. Натисніть "➕ Додати товар"
2. Назва: "Милий кіт"
3. Категорія: "🧸 Іграшка"
4. Emoji: "🐱"
5. Ціна: "150"
6. Матеріал: "PLA · один колір"
7. Перетягніть 3 фото
8. Натисніть "🔥 Гарячий товар"
9. Натисніть "Зберегти товар"

✅ Товар додано! ID: 13
```

### Приклад 2: Додавання персоналізованого товару

```
1. Натисніть "➕ Додати товар"
2. Назва: "Брелок з номером авто"
3. Категорія: "🎨 Персоналізований"
4. Emoji: "🚗"
5. Ціна: "80"
6. Матеріал: "PLA · персоналізований"
7. Поле для вводу: "Номер автомобіля (наприклад: АВ 1234 СD)"
8. Натисніть "Зберегти товар"

✅ Товар додано! ID: 101
```

### Приклад 3: Редагування з акцією

```
1. Натисніть "✏️ Редагувати" на товарі
2. Заповніть "Стара ціна: 500"
3. Змініть "Ціна: 350"
4. Натисніть "Зберегти товар"

✅ Акція встановлена! Знижка: 150 ₴ (30%)
```

---

## 🐛 Вирішення проблем

### Проблема: Фото не завантажуються

**Рішення:**
1. Перевірте Cloudinary ключі в `.env`
2. Перевірте Cloudinary акаунт (чи активний)
3. Перевірте розмір фото (максимум 25 MB)
4. Перевірте лог в консолі для помилок

### Проблема: Товар не зберігається

**Рішення:**
1. Перевірте консоль браузера (F12 → Console)
2. Перевірте, чи всі обов'язкові поля заповнені
3. Перевірте серверні логи
4. Спробуйте перезавантажити сторінку

### Проблема: `/admin` команда не видна в меню

**Рішення:**
1. Перезавантажте бот: `python bot.py`
2. Натисніть `/` у Telegram для оновлення меню
3. Перевірте OWNER_ID в `.env`

---

## 📚 Корисні посилання

- [Cloudinary Dashboard](https://cloudinary.com/console)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [python-telegram-bot Docs](https://python-telegram-bot.readthedocs.io/)
- [aiohttp Docs](https://docs.aiohttp.org/)

---

## 📞 Підтримка

**При виникненні проблем:**
1. Перевірте логи (консоль при запуску)
2. Читайте розділ "Вирішення проблем" вище
3. Перевірте API endpoints вручну через cURL або Postman
4. Оновіть залежності: `pip install -r requirements.txt --upgrade`

---

**Версія:** 1.0  
**Останнє оновлення:** 2026-05-18  
**Статус:** ✅ Готово до використання
